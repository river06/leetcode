package HW;
import java.util.*;

public class triangle {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		ArrayList<ArrayList<Integer>> triangle = new ArrayList<ArrayList<Integer>>(3);
		for( int i=0; i<triangle.size(); i++) {
			ArrayList<Integer> tmp = new ArrayList<Integer>(i+1);
			triangle.get(i).addAll(tmp);
		}
		
		for( int i=0; i<triangle.size(); i++) {
			for (int j=0; i<triangle.get(i).size(); j++) {
				triangle.set(int index, E element)
			}
			
		}
		

	}

}
